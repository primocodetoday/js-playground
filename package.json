{
  "name": "js-playground",
  "version": "1.0.0",
  "description": "Javascript Training Repository",
  "main": "index.js",
  "dependencies": {
    "eslint-config-airbnb": "^18.1.0",
    "lint-staged": "^10.1.3",
    "prettier": "^2.0.4"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.1",
    "husky": "^4.2.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Primoza",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "prettier --config .prettierrc --write",
      "eslint --fix"
    ]
  }
}
